server {
  server_name dev.topix.im;
  location ~ /\.git {
    deny all;
  }
  location /{
    root /home/chen/repo/Topix;
    autoindex on;
    index index.html;
  }
}

server {
  server_name index.topix.im topix.im;
  location ~ /\.git {
    deny all;
  }
  location / {
    root /home/chen/repo/Topix/topix.im/;
    autoindex on;
    index index.html;
  }
}

server {
  server_name logo.topix.im;
  location ~ /\.git {
    deny all;
  }
  location / {
    root /home/chen/repo/Topix/logo/;
    autoindex on;
    index index.html;
  }
}
